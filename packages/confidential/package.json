{
    "name": "@shadow-protocol/confidential",
    "version": "0.1.0",
    "private": true,
    "scripts": {
      "build": "cargo build",
      "test": "cargo test",
      "clean": "rm -rf target",
      "deploy": "echo 'Deploy with arcium deploy'"
    },
    "devDependencies": {
      "@types/node": "^20.0.0",
      "typescript": "^5.0.0"
    }
  }